# ═══════════════════════════════════════════════════════════════════
# Massi-Bot Chatbot Engine — Environment Variables
# ═══════════════════════════════════════════════════════════════════
# Copy this file to .env and fill in your values:
#   cp .env.template .env
#
# Claude Code will walk you through each value when you run:
#   claude --dangerously-skip-permissions
# ═══════════════════════════════════════════════════════════════════

# ─── PLATFORM CHOICE ─────────────────────────────────────────────
# Which platform(s) to run: "fanvue", "onlyfans", or "both"
PLATFORM_MODE=fanvue

# ─── DOMAIN + SSL ────────────────────────────────────────────────
# Your domain for webhooks and OAuth (e.g., api.yourdomain.com)
# Must have an A record pointing to this VM's public IP
DOMAIN=

# ─── SUPABASE (Free Tier) ───────────────────────────────────────
# Create project at: https://supabase.com/dashboard
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_PUBLISHABLE_KEY=

# ─── LLM — OpenRouter ───────────────────────────────────────────
# Sign up at: https://openrouter.ai
# Add $50 credits (cost ~$0.001-0.002 per message with tiered models)
OPENROUTER_API_KEY=
LLM_PROVIDER=venice

# ─── PER-AGENT MODEL OVERRIDES (optional) ───────────────────────
# Each agent defaults to an optimized model. Override here if needed.
# EMOTION_MODEL=mistralai/mistral-small-3.1-24b-instruct
# STRATEGIST_MODEL=google/gemini-2.5-flash
# DIRECTOR_MODEL=anthropic/claude-haiku-4-5-20251001
# GFE_MODEL=anthropic/claude-haiku-4-5-20251001
# MEDIA_REACTOR_MODEL=mistralai/mistral-small-3.1-24b-instruct
# VALIDATOR_MODEL=mistralai/mistral-small-3.1-24b-instruct
# OPUS_MODEL=anthropic/claude-sonnet-4-6

# ─── EMBEDDING MODEL (optional) ─────────────────────────────────
# Default: BGE-M3 (1024-dim, ~570MB, much better retrieval than MiniLM)
# To use the smaller/faster MiniLM: EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
# EMBEDDING_MODEL=BAAI/bge-m3

# ─── TELEGRAM ADMIN BOT ─────────────────────────────────────────
# Create bot via @BotFather on Telegram → /newbot
# Get your chat ID via @userinfobot
TELEGRAM_BOT_TOKEN=
TELEGRAM_ADMIN_IDS=

# ─── FANVUE (if PLATFORM_MODE is "fanvue" or "both") ────────────
# Register OAuth app at: https://fanvue.com/developers
# Redirect URI: https://{DOMAIN}/oauth/callback
FANVUE_CLIENT_ID=
FANVUE_CLIENT_SECRET=
FANVUE_APP_ID=
FANVUE_WEBHOOK_SECRET=
FANVUE_API_VERSION=2025-06-26
FANVUE_API_BASE=https://api.fanvue.com
FANVUE_AUTH_BASE=https://auth.fanvue.com
# UUID of the model on Fanvue (from Supabase models table)
FANVUE_MODEL_ID=
# JSON map of IG handles to avatar persona IDs
# Example: {"@your_ig":"luxury_baddie"}
FANVUE_IG_MAP={}

# ─── ONLYFANS (if PLATFORM_MODE is "onlyfans" or "both") ───────
# Sign up at: https://app.onlyfansapi.com
OFAPI_KEY=
OFAPI_ACCOUNT_ID=
OFAPI_WEBHOOK_SECRET=
OFAPI_BASE=https://app.onlyfansapi.com
# UUID of the model (from Supabase models table)
OF_MODEL_ID=
# OnlyFans user ID of the model account (for /register_of media pull)
OF_MODEL_CHAT_ID=
# JSON map of IG handles to avatar persona IDs
OF_IG_MAP={}

# ─── REDIS (runs in Docker — no changes needed) ─────────────────
REDIS_URL=redis://redis:6379/0

# ─── SENTRY (Optional — free tier) ──────────────────────────────
# Create project at: https://sentry.io (choose Python)
SENTRY_DSN=
ENVIRONMENT=production

# ─── CONTENT MODE ────────────────────────────────────────────────
# Set to "gfe_only" if you cannot create NSFW content
# This disables the selling pipeline and uses $20 continuation paywalls only
# CONTENT_MODE=full
